apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: agent-flow-system
  labels:
    app.kubernetes.io/name: agentflow-redis
    app.kubernetes.io/component: cache
spec:
  selector:
    app.kubernetes.io/name: agentflow-redis
  ports:
  - name: redis
    port: 6379
    targetPort: 6379