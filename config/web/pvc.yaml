---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: task-outputs
  namespace: agent-flow-system
  labels:
    app.kubernetes.io/name: agent-flow
    app.kubernetes.io/component: outputs
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
